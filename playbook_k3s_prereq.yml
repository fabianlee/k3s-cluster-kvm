---

# Our own ubuntu prereq: os packages, settings, binaries
- hosts: k3s_cluster
  gather_facts: yes
  become: yes
  roles:
    - k3s-prereq

# Prereq needed by 'k3s-ansible' project
- hosts: k3s_cluster
  gather_facts: yes
  become: yes
  roles:
    - prereq 
    - download
